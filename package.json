{
  "name": "slides",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install": "cd client && pnpm i && cd ../server && pnpm i",
    "client:dev": "cd client && pnpm dev",
    "client:build": "cd client && pnpm build",
    "client:serve": "cd client && pnpm serve",
    "server:dev": "cd server && direnv exec ./ uvicorn main:app --reload --port 3001",
    "dev": "concurrently \"pnpm client:dev\" \"pnpm server:dev\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^9.0.1"
  }
}
